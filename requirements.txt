flask>=3.0
waitress>=3.0
matplotlib>=3.8   # server-rendered snow-profile figure
python-dotenv>=1.0  # optional: .env support
pillow-heif>=0.15   # iPhones shoot HEIC by default; converted to JPEG on upload
